# VeriRank AI - Environment Configuration

# Gemini API Key (Required if not entered via web UI)
GEMINI_API_KEY=your_gemini_api_key_here

# SQLite Database path (defaults to verirank_logs.db)
DATABASE_PATH=verirank_logs.db
